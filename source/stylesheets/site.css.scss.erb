body {
  background: url(<%= image_path('background.png') %>);
}

table {
  background: url(<%= image_path('background-flag.png') %>);
}

h1 {
  color: greenyellow;
  font-size: 40px;
}

#logo {
  width: 200px;
  img {
    width: 200px;
  }
}

#chicken {
  position: absolute;
  transform: rotate(-180deg);
  margin-top: 35px;
}
